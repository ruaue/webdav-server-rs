name: Build webdav-server for x86_64-unknown-linux-musl
on:
  workflow_dispatch:

jobs:
  build_and_test:
    name: Rust project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install musl-tools
        run: |
          sudo apt-get update -y
          sudo apt-get install musl-tools -y
          sudo apt-get install libpam0g-dev libntirpc-dev -y

      - name: Install rust
        run: |
          rustup target add x86_64-unknown-linux-musl 

      - name: Build
        run: cargo build --verbose --release --all-features  --target x86_64-unknown-linux-musl
      
      - name: Upload release
        uses: softprops/action-gh-release@v0.1.15
        with:
           tag_name: ${{ github.ref_name }}
           files: |
               target/release/webdav-server
